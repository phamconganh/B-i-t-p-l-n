
#include "loadheart.h"

/*Heart::Heart(Painter& painter_): painter(painter_)
{
    loadGamePictures();
}

Heart::~Heart()
{
    for (SDL_Texture* texture : pictures)
        SDL_DestroyTexture(texture);
}

void Heart::loadGamePictures()
{
    pictures.push_back(painter.loadTexture("./image/red heart.bmp"));
    pictures.push_back(painter.loadTexture("./image/black heart.bmp"));
    pictures.push_back(painter.loadTexture("./image/break heart.bmp"));
}*/
